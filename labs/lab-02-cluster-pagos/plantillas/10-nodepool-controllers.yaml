# Plantilla — pool de controllers. Trabaja sobre una COPIA, nunca sobre la
# plantilla (ver la guía 02). Completa los TODO.
apiVersion: kafka.strimzi.io/v1
kind: KafkaNodePool
metadata:
  name: controllers
  labels:
    strimzi.io/cluster: pagos
spec:
  # TODO A: número de réplicas del pool de controllers.
  # Pista: topología económica de laboratorio (guía 01). En producción serían 3.
  replicas: # TODO
  # TODO B: rol de este pool. Valores admitidos: controller, broker.
  roles:
    - # TODO
  resources:
    requests:
      cpu: 250m
      memory: 512Mi
    limits:
      cpu: "1"
      memory: 512Mi
  jvmOptions:
    -Xms: 256m
    -Xmx: 256m
  storage:
    type: jbod
    volumes:
      - id: 0
        type: ephemeral
        kraftMetadata: shared
